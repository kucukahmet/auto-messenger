version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "db/schema.sql"
    queries:
      - "db/queries"
    gen:
      go:
        package: "repo"
        out: "internal/repo"
        emit_json_tags: true
        emit_interface: false
        sql_package: "pgx/v5"
